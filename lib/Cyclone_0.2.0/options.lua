--options

local options = {
["exposepause"] = true,        -- Exports the pause functions for other mods to use.
["terminalkey"] = "tab",       -- The key that quickly opens/closes the terminal.
["fade-cursor"] = 5,           -- The seconds it takes for the cursor to fade away.
["fix"] = true,                -- Fixes breakable blocks not breaking on certain attacks.
["real_damage"] = false,       -- Makes damagers show the real damage they deal.
["no-launcher"] = false,       -- Disables the launcher.
["launcher-terminal"] = false, -- Puts the terminal in the launcher.
}

return options